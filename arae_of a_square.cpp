#include <stdio.h>
#include <math.h>

int a,b;

int main(){
	printf("enter the lenght of the side:");
	scanf("%d",&a);
	b=pow(a,2);
	printf("The area of the square is=%d",b);
	return 0;
}
